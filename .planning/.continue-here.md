---
context: default
phase: "6 — Frutiger Aero Look & Installer"
status: paused
last_updated: "2026-05-19T06:36:24.098Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

<current_state>
Phases 1–6 (code) completas. App funciona: download YouTube/Twitter/Instagram/
Facebook/TikTok, MP4/MP3, preview de thumbnail, progresso multi-estágio,
cancelamento limpo, batch download via .txt, UI Frutiger Aero.

Pendente desta sessão: commit das alterações abaixo + instalador (deferred).
</current_state>

<completed_work>

Esta sessão (não commitado ainda):
- **Batch download** — botão `···` ao lado do campo de URL abre OpenFileDialog para .txt
  com uma URL por linha; faz download sequencial mostrando `[2/7] Baixando vídeo... 65%`;
  Cancelar para no item atual e mostra resultado parcial; digitar URL manual descarta o batch
- **Fix output folder** — removido o subfolder `VidDrop`; arquivos vão direto para `Downloads`
- **Fix `···` crash** — `[STAThread]` ausente em `Program.cs` causava `ThreadStateException`
  ao abrir `OpenFileDialog`; corrigido com `Main()` explícito

Sessões anteriores (commitados):
- Phases 1–3: download YouTube, picker MP3/MP4 com qualidade/bitrate, preview thumbnail
- Phase 4: todas as plataformas + badge colorido + erros específicos por plataforma
- Phase 5: progresso multi-estágio (Vídeo/Áudio/Finalizando), toggle Baixar↔Cancelar
- Phase 6 UI: sky gradient, glass card com gloss, AeroButton aqua, AeroProgressBar, SelfUpdater
</completed_work>

<remaining_work>

1. **Commit das alterações não commitadas** (Core/DownloadEngine.cs, Program.cs, UI/MainForm.cs)
2. **Adicionar Tools/** — yt-dlp.exe e ffmpeg.exe precisam ser colocados em `Tools/`
   (não estão no repo; app crasha no startup sem eles)
3. **Instalador** — deferred pelo usuário até o app estar 100% pronto:
   - `dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true`
   - Abrir `Installer/VidDrop.iss` no Inno Setup 6.x → Compile
   - Output: `dist/VidDrop_Setup.exe`
4. **Phase 7** (futuro): Auto-Cut viral clip generator com IA
</remaining_work>

<decisions_made>

- Output folder: `Downloads` direto (sem subfolder VidDrop) — decisão do usuário
- Instalador: deferred para quando o app estiver 100% finalizado
- Frutiger Aero: gradiente de céu + glass card + AeroButton aqua com gloss de 45%
- Batch: arquivo .txt com uma URL por linha, download sequencial (não paralelo)
- `[STAThread]`: exige `Main()` explícito — top-level statements não suportam o atributo
</decisions_made>

<blockers>

- **Tools/ vazio**: yt-dlp.exe e ffmpeg.exe não estão no repo. O app falha no startup
  com "Instalação corrompida" se os binários não estiverem em `Tools/`.
  → Solução: baixar yt-dlp.exe de github.com/yt-dlp/yt-dlp e ffmpeg.exe (LGPL build)
    e colocar em `C:\Users\arthur\Documents\downloader\Tools\`
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — status atual das fases
2. `.planning/ROADMAP.md` — fases e requisitos completos
3. `CLAUDE.md` — arquitetura, constraints e stack

## Infrastructure State
- App: `bin\Debug\net8.0-windows\win-x64\VidDrop.exe` (build Debug atual)
- Tools/: **VAZIO** — binários ausentes (ver blockers)
- Instalador: `Installer/VidDrop.iss` existe mas nunca foi compilado

<context>
Sessão focada em finalizar phases 4-6 e adicionar batch download. O usuário
quer um app de distribuição real (instalador .exe). A UI Frutiger Aero está
funcionando com glass card e controles glossy. O próximo passo natural é
commitar o que ficou pendente, adicionar os binários em Tools/, e então montar
o instalador quando o usuário der o ok.
</context>

<next_action>
1. Commitar alterações pendentes: `git add Core/DownloadEngine.cs Program.cs UI/MainForm.cs .planning/ && git commit`
2. Verificar se Tools/ tem yt-dlp.exe e ffmpeg.exe (se não, usuário precisa adicioná-los)
3. Testar batch download: criar .txt com 2-3 URLs, clicar `···`, selecionar arquivo, Baixar
4. Quando usuário confirmar "pronto", montar o instalador
</next_action>
